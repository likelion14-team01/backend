spring:
  application:
    name: collab_session
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

cors:
  #  TODO : 실제 배포된 프론트 주소 변경할것
  allowed-origins: http://localhost:3000,http://localhost:5173,https://your-react-app.vercel.app
